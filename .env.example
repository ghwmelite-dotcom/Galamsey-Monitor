# Cloudflare D1 Database
# D1 database is configured in wrangler.toml
# To create: wrangler d1 create galamsey-db
# Update the database_id in wrangler.toml after creation

# Authentication (NextAuth.js)
# Set these as secrets: wrangler pages secret put NEXTAUTH_SECRET
NEXTAUTH_SECRET=your-super-secret-key-change-in-production
NEXTAUTH_URL=http://localhost:3000

# Email Service (Resend)
RESEND_API_KEY=re_your_resend_api_key
FROM_EMAIL=Galamsey Monitor <notifications@galamsey.gov.gh>

# SMS Service (Twilio)
TWILIO_ACCOUNT_SID=ACyour_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Optional: Admin email for notifications
ADMIN_EMAIL=admin@galamsey.gov.gh

# ============================================
# Cloudflare Setup Instructions
# ============================================
#
# 1. Install wrangler: npm install -g wrangler
#
# 2. Login to Cloudflare: wrangler login
#
# 3. Create D1 database:
#    wrangler d1 create galamsey-db
#    (Update database_id in wrangler.toml with the returned ID)
#
# 4. Create R2 bucket:
#    wrangler r2 bucket create galamsey-uploads
#
# 5. Push schema to D1:
#    wrangler d1 execute galamsey-db --file=./scripts/d1-schema.sql
#
# 6. Seed initial data:
#    wrangler d1 execute galamsey-db --file=./scripts/d1-seed.sql
#
# 7. Set secrets:
#    wrangler pages secret put NEXTAUTH_SECRET
#    wrangler pages secret put NEXTAUTH_URL
#
# 8. Build and deploy:
#    npm run build
#    npm run deploy
#
